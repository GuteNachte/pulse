param()

$ErrorActionPreference = "Stop"

Write-Host "Pulse Agent installation is managed by Pulse Hub." -ForegroundColor Cyan
Write-Host "Open Pulse -> Clients -> Add system, then copy the generated Windows command." -ForegroundColor Yellow
Write-Host "The generated command downloads the matching pulse-agent release from your Hub."
