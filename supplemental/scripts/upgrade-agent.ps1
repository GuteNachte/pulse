param()

$ErrorActionPreference = "Stop"

Write-Host "Pulse Agent updates are managed by Pulse Hub." -ForegroundColor Cyan
Write-Host "Open Pulse -> Settings -> Agent Management, then click Update for the target client." -ForegroundColor Yellow
Write-Host "The Hub will provide the matching pulse-agent release for the current version."
